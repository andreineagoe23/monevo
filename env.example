# Copy this file to `.env` (same directory) to customize Docker Compose defaults.

# --- Docker Compose defaults ---
POSTGRES_DB=monevo
POSTGRES_USER=monevo
POSTGRES_PASSWORD=change-me

# Exposed ports on your machine (change if you have conflicts)
POSTGRES_PORT=5432
REDIS_PORT=6379
BACKEND_PORT=8000
FRONTEND_PORT=3000

# Django
DJANGO_DEBUG=True
DJANGO_SECRET_KEY=django-insecure-change-me
DJANGO_ALLOWED_HOSTS_CSV=localhost,127.0.0.1
TIME_ZONE=UTC

# Used for emails/redirects generated by the backend
FRONTEND_URL=http://localhost:3000

# Frontend ↔ backend (CORS/CSRF)
CORS_ALLOWED_ORIGINS_CSV=http://localhost:3000,http://127.0.0.1:3000
CSRF_TRUSTED_ORIGINS_CSV=http://localhost:3000,http://127.0.0.1:3000


