# Copy to `frontend/.env` (CRA reads REACT_APP_* at build time).
#
# If you run the frontend via Docker, these can also be provided as build args:
# - REACT_APP_BACKEND_URL
# - REACT_APP_BACKEND_PORT
# - REACT_APP_CHECKOUT_URL

# Optional: hardcode the API base URL (defaults to window.location + /api)
# REACT_APP_BACKEND_URL=http://localhost:8000/api

# Optional: if running on localhost but backend is not 8000
# REACT_APP_BACKEND_PORT=8000

# Optional: override checkout link for premium upsells
# REACT_APP_CHECKOUT_URL=https://pay.monevo.app/checkout


