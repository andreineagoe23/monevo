# Copy to `backend/.env` if you want `settings/settings.py` to load values from a file locally.
# Docker Compose users: prefer the root `.env` (or `env.example` → `.env`) instead.

DEBUG=True
SECRET_KEY=django-insecure-change-me
TIME_ZONE=UTC

# DB / Redis (Docker Compose sets these automatically; keep for non-docker workflows)
DATABASE_URL=postgresql://monevo:monevo@localhost:5432/monevo
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/0

ALLOWED_HOSTS_CSV=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS_CSV=http://localhost:3000,http://127.0.0.1:3000
CSRF_TRUSTED_ORIGINS_CSV=http://localhost:3000,http://127.0.0.1:3000


